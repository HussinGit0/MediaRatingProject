namespace MediaRatingProject.Data.Enums
{
    /// <summary>
    /// This enum is currently unused. But as an optional feature for the full project, the age restriction can display as this enum depending on the age range.
    /// </summary>
    public enum AgeRestriction
    {
        Everyone,
        Teen,
        Mature,
        Adult
    }
}
